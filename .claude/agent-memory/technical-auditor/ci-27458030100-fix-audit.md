---
name: ci-27458030100-fix-audit
description: Audit of CI run 27458030100 fixes (app.rs clippy match-guard/map_or rewrite + e2e ipc.ts waitUntil hydration race) — APPROVE
metadata:
  type: project
---

# CI run 27458030100 fix audit (2026-06-13, head a729c95)

Verdict: APPROVE (one LOW imprecision in the claim's clippy framing; no behavioral defects).

## Clippy failure (App Shell job 81166183600)
- Rust/clippy 1.96.0 on CI; dev box is 0.1.93 (confirmed `clippy-driver --version`).
- Exactly 6 errors: 1x collapsible_match (app.rs:431) + 5x map_unwrap_or (513,517,591,672,676). Verified by counting distinct help-link occurrences; "could not compile ... due to 6 previous errors".
- VERIFIED via /tmp synthetic probes on local clippy 0.1.93:
  - 0.1.93 DOES flag map_unwrap_or (same lint+map_or suggestion). So claim "0.1.93 missed [all 6]" is IMPRECISE — it only misses collapsible_match.
  - 0.1.93 does NOT flag collapsible_match on the full-arm-body boolean-`if`+or-pattern+`_` shape; 1.96 does. This is the genuine version gap.
- Fixed working tree passes local clippy 0.1.93 clean (webkit2gtk present on this Gentoo box; "Checking luminos-app" ran, exit 0).
- map_or arg order correct in all 5 sites: `map_or(default, f)` == old `map(f).unwrap_or(default)`. Defaults are cheap literals so eager eval is fine.
- match-guard rewrite semantically equivalent: guard `if cxchg.is_ok()` false => arm doesn't match => falls to `_ => {}` no-op. compare_exchange still runs once per Exit event; AcqRel/Acquire unchanged. Teardown once-only preserved.

## E2E failure (job 81166183599) — PRE-EXISTING harness defect, NOT a regression
- App booted fine: `managed_state_ok: zoom='2' config_present='true'`, `surface_created`, two `page-*` handles, both probed `RESULT false`. Failure is `switchToControlPanel` throw "control panel never loaded" at ipc.ts:108 in the before-all hook.
- E2E build step DID pass `--features "tauri custom-protocol"` (verified in log).
- Slider genuinely gated behind async hydration: useSettingsStore initial `isHydrating:true`; HydrationGate shows "Loading settings…" until false; App.tsx PanelRoot useEffect getCurrentSettings -> hydrate (success OR catch->hydrate(DEFAULT_SETTINGS)) flips false. So slider WILL render even on IPC failure => waitUntil(30s) cannot hang forever on healthy-but-slow boot. Shell->MagnificationPage->ZoomLevelSlider `<input type="range">` matches probe selector; magnification is default page.
- test-e2e job has NEVER been green: every E04-era run failed (27458030100, 27455821073, 27076576721, 27075224925). Pre-E04 "success" runs had no e2e job. = documented DC-13 blind spot.
- Prior run 27455821073 (commit 9cad09b, BEFORE db3ecff runtime fixes) shows IDENTICAL failure (same log shape, same ipc.ts:108). Proves failure predates and is independent of the runtime-fix commits.

## Residual / unproven until next CI run
- The waitUntil E2E fix cannot be proven locally (no WebKitWebDriver/tauri-driver on dev box; tsc --noEmit passes only). Proof = next test-e2e CI run going green.
- Clippy fix: fully provable locally on the FIXED tree (passes 0.1.93). The 1.96-only collapsible_match catch is the only thing not reproducible on the dev box clippy.

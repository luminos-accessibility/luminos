# Generated by tauri-specta (story E04/005). Do not reformat — the CI
# bindings-up-to-date check diffs this file against a fresh `cargo`-driven
# export, so prettier reformatting it would make that check fail. ESLint already
# ignores it via `globalIgnores` in eslint.config.js; mirror that for prettier.
src/ipc/bindings.ts
dist
coverage
